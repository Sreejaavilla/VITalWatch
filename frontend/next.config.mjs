/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel root directory is `frontend`, so no basePath juggling.
  // NEXT_PUBLIC_API_URL and NEXT_PUBLIC_STUB_MODE come from Vercel project env vars.
}
export default nextConfig
