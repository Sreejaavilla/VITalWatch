// Alerts. OWNER: Ishan.
// Phase 0 placeholder so the route resolves and the build passes.
// Backing endpoints: GET /api/alerts · POST /api/alerts/:id/ack

export default function AlertsPage() {
  return (
    <main style={{ padding: 32, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Alerts</h1>
      <p style={{ color: '#666' }}>Placeholder — Phase 1.</p>
      <p style={{ color: '#666', fontFamily: 'ui-monospace, monospace', fontSize: 13 }}>GET /api/alerts · POST /api/alerts/:id/ack</p>
    </main>
  )
}
