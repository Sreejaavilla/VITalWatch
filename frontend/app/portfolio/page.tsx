// Portfolio. OWNER: Ishan.
// Phase 0 placeholder so the route resolves and the build passes.
// Backing endpoints: GET /api/kpi/portfolio · GET /api/studies · GET /api/alerts

export default function PortfolioPage() {
  return (
    <main style={{ padding: 32, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Portfolio</h1>
      <p style={{ color: '#666' }}>Placeholder — Phase 1.</p>
      <p style={{ color: '#666', fontFamily: 'ui-monospace, monospace', fontSize: 13 }}>GET /api/kpi/portfolio · GET /api/studies · GET /api/alerts</p>
    </main>
  )
}
