// Login. OWNER: Ishan.
// Phase 0 placeholder so the route resolves and the build passes.
// Backing endpoints: POST /auth/login · GET /auth/me

export default function LoginPage() {
  return (
    <main style={{ padding: 32, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Login</h1>
      <p style={{ color: '#666' }}>Placeholder — Phase 1.</p>
      <p style={{ color: '#666', fontFamily: 'ui-monospace, monospace', fontSize: 13 }}>POST /auth/login · GET /auth/me</p>
    </main>
  )
}
