// Pharmacovigilance. OWNER: Ishan.
// Phase 0 placeholder so the route resolves and the build passes.
// Backing endpoints: POST /api/ae · GET /api/ae · POST /api/coding/suggest · GET /api/signals

export default function AEPage() {
  return (
    <main style={{ padding: 32, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Pharmacovigilance</h1>
      <p style={{ color: '#666' }}>Placeholder — Phase 1.</p>
      <p style={{ color: '#666', fontFamily: 'ui-monospace, monospace', fontSize: 13 }}>POST /api/ae · GET /api/ae · POST /api/coding/suggest · GET /api/signals</p>
    </main>
  )
}
