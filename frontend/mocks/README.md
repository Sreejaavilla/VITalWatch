# Mocks — generated, never hand-edited

Synced from `contracts/fixtures/` by `npm run sync-mocks`.

Hand-editing a file here makes the frontend and the backend disagree about shape,
and that disagreement only surfaces when STUB_MODE is switched off — at hour 20,
under pressure. Regenerate instead.
