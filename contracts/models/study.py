"""Study — the portfolio unit. OWNER: Kavin."""

# Lifecycle: protocol -> ec_approval -> ctri_registered -> site_activation
#            -> screening -> enrolling -> follow_up -> close_out
# Fields to define: id, title, protocol_no, ctri_number, phase, status,
#   ec_approval_date, ec_expiry_date, ctri_registration_date,
#   target_enrolment, actual_enrolment, pi_id, site_ids, start_date, end_date
